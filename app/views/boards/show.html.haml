.container
  .tabs
    %h1.board_title= @board.title
  .board
    .board_content
      = @board.content
    .board_detail
      = image_tag @board.user.avatar_image
      %div
        %p= @board.author_name
        %p= @board.display_created_at
